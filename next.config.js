/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    output: "export",
    basePath: "/Sapo-Github",
    images: {
          unoptimized: true,
    },
};

module.exports = nextConfig;
